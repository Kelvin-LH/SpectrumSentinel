services:
  spectrum-sentinel:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./models:/app/models
    environment:
      SPECTRUM_SENTINEL_MODEL_DIR: /app/models
    restart: unless-stopped
